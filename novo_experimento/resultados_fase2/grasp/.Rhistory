pwd()
getwd()
setwd("C:\Users\Italo Silva\Projeto\faptp\novo_experimento")
setwd("C:\\Users\\Italo Silva\\Projeto\\faptp\\novo_experimento")
getwd()
list.files('.')
setwd("C:\\Users\\Italo Silva\\Projeto\\faptp\\novo_experimento\resultados")
setwd("C:\\Users\\Italo Silva\\Projeto\\faptp\\novo_experimento\\resultados")
getwd()
list.files('.')
result
library(plyr)
le.arquivos <- function(caminho) {
arquivos <- list.files(caminho)
ldply(arquivos, read.csv)
}
rpd <- function(f.method, f.best) {
abs((f.method - f.best)/f.best) * 100
}
rpd.reduce <- function(f, col, f.best) {
f(sapply(col, function(cur) rpd(cur, f.best)))
}
frames <- le.arquivos('.')
result <- ddply(frames, ~ID.Algoritmo, summarise,
Media.FO=mean(FO), Mediana.FO=median(FO), Max.FO=max(FO),
Media.Tempo=mean(Tempo.total), Mediana.Tempo=median(Tempo.total), Min.Tempo=min(Tempo.total),
RPD.FO=(function(x) { rpd.reduce(mean, x, max(frames$FO)) })(FO),
RPD.Tempo=(function(x) { rpd.reduce(mean, x, min(frames$Tempo.total)) })(Tempo.total)
)
result
result <- ddply(.(RPD.FO))
result <- ddply(result, .(RPD.FO))
result <- ddply(result, .(RPD.FO))
result
top_n
filter(row_number() <= 10)
filter(result, row_number() <= 10)
filter(result, rows=10
)
type
typeof
typeof(result)
result[10]
result
result[:10]
result[1:10]
typeof(result)
slice
slice(result, 5)
data.frame(result)
result <- data.frame(result)
typeof(result)
head(result, 4)
library(plyr)
le.arquivos <- function(caminho) {
arquivos <- list.files(caminho)
ldply(arquivos, read.csv)
}
rpd <- function(f.method, f.best) {
abs((f.method - f.best)/f.best) * 100
}
rpd.reduce <- function(f, col, f.best) {
f(sapply(col, function(cur) rpd(cur, f.best)))
}
frames <- le.arquivos('.')
result <- ddply(frames, ~ID.Algoritmo, summarise,
Media.FO=mean(FO), Mediana.FO=median(FO), Max.FO=max(FO),
Media.Tempo=mean(Tempo.total), Mediana.Tempo=median(Tempo.total), Min.Tempo=min(Tempo.total),
RPD.FO=(function(x) { rpd.reduce(mean, x, max(frames$FO)) })(FO),
RPD.Tempo=(function(x) { rpd.reduce(mean, x, min(frames$Tempo.total)) })(Tempo.total)
)
result <- ddply(result, .(RPD.FO), .(RPD.Tempo))
head(result, 5)
library(plyr)
le.arquivos <- function(caminho) {
arquivos <- list.files(caminho)
ldply(arquivos, read.csv)
}
rpd <- function(f.method, f.best) {
abs((f.method - f.best)/f.best) * 100
}
rpd.reduce <- function(f, col, f.best) {
f(sapply(col, function(cur) rpd(cur, f.best)))
}
frames <- le.arquivos('.')
result <- ddply(frames, ~ID.Algoritmo, summarise,
Media.FO=mean(FO), Mediana.FO=median(FO), Max.FO=max(FO),
Media.Tempo=mean(Tempo.total), Mediana.Tempo=median(Tempo.total), Min.Tempo=min(Tempo.total),
RPD.FO=(function(x) { rpd.reduce(mean, x, max(frames$FO)) })(FO),
RPD.Tempo=(function(x) { rpd.reduce(mean, x, min(frames$Tempo.total)) })(Tempo.total)
)
result <- ddply(result, .(RPD.FO))
head(result, 5)
result
list.files('.')
arq <- list.files('.')
arq
size
arq.len
typeof(arq)
arq
list.files('.')
length(arq)
frames
length(frames)
length(frames, 1)
length(frames)
typeof(frames)
rows
rows(frames)
frame
frames
3970+2500
head(result, 5)
library(plyr)
le.arquivos <- function(caminho) {
arquivos <- list.files(caminho)
ldply(arquivos, read.csv)
}
rpd <- function(f.method, f.best) {
abs((f.method - f.best)/f.best) * 100
}
rpd.reduce <- function(f, col, f.best) {
f(sapply(col, function(cur) rpd(cur, f.best)))
}
frames <- le.arquivos('.')
result <- ddply(frames, ~ID.Algoritmo, summarise,
Media.FO=mean(FO), Mediana.FO=median(FO), Max.FO=max(FO),
Media.Tempo=mean(Tempo.total), Mediana.Tempo=median(Tempo.total), Min.Tempo=min(Tempo.total),
RPD.FO=(function(x) { rpd.reduce(mean, x, max(frames$FO)) })(FO),
RPD.Tempo=(function(x) { rpd.reduce(mean, x, min(frames$Tempo.total)) })(Tempo.total)
)
result <- ddply(result, .(RPD.FO))
head(result, 5)
max(result$Max.FO)
result[Max.FO = 22651.4]
result[$Max.FO = 22651.4]
result[Max.FO == 22651.4]
result[result$Max.FO == 22651.4]
result[result$FO == 22651.4]
result
result[result$Max.FO == 22651.4]
result[result$Max.FO == 22651.4,]
head(result, 5)
tail(result, 5)
head
head(result, 5)
setwd('C:\Users\Italo Silva\Projeto\faptp\novo_experimento\resultados_fase2')
zz <- readClipboard()
zz
setwd(zz)
getwd(0)
getwd()
library(plyr)
le.arquivos <- function(caminho) {
arquivos <- list.files(caminho)
ldply(arquivos, read.csv)
}
rpd <- function(f.method, f.best) {
abs((f.method - f.best)/f.best) * 100
}
rpd.reduce <- function(f, col, f.best) {
f(sapply(col, function(cur) rpd(cur, f.best)))
}
frames <- le.arquivos('.')
result <- ddply(frames, ~ID.Algoritmo, summarise,
Media.FO=mean(FO), Mediana.FO=median(FO), Max.FO=max(FO),
Media.Tempo=mean(Tempo.total), Mediana.Tempo=median(Tempo.total), Min.Tempo=min(Tempo.total),
RPD.FO=(function(x) { rpd.reduce(mean, x, max(frames$FO)) })(FO),
RPD.Tempo=(function(x) { rpd.reduce(mean, x, min(frames$Tempo.total)) })(Tempo.total)
)
result <- ddply(result, .(RPD.FO))
head(result, 5)
result
